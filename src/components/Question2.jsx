import React from "react";

function Question_Two() {
  return (
    <p>
      The first one is the default that the page contains only one export
      default and we can get it like this <b>import Something from 'package' </b>
      and <b>import {"Something"} from 'package</b> is that the page contains
      multiples export but we can get it as the same name.
    </p>
  );
}

export { Question_Two };
